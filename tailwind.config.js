/** @type {import('tailwindcss').Config} */
export default {
  darkMode: "class", // dark라는 클래스 유무에 따라서 다크모드가 된다.
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
